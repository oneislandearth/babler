// Import the required node modules
const { execSync } = require('child_process');

// Import the babel configurator
const { babelConfig } = require('./config');

// Export the functions
module.exports = {
  babelConfig
}